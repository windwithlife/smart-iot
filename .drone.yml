kind: pipeline
name: build-iot-test

steps:
  - name: test
    image: maven:3-jdk-10
    commands:
      - cd ./service
      - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
trigger:
  branch:
    - master
  event:
    - push